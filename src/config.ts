/** Paste your Google Apps Script web-app URL here (see backend.gs). Empty = claude.ai artifact storage / this device only. */
export const BACKEND_URL = '/api/public/trip';
/** The public link you share on WhatsApp — used in the nudge message. Empty = the page's own URL. */
export const SHARE_URL = '';
/** Answers-by date shown in the header (YYYY-MM-DD). Empty hides it. */
export const VOTE_BY = '2026-09-03';
